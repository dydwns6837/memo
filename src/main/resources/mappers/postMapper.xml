<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "../mappers/mybatis-3-mapper.dtd">
 
<mapper namespace="com.memo.post.mapper.PostMapper">
	<select id="selectPostList" resultType="map">
		SELECT
			*
		FROM
			`post`
	</select>
</mapper>